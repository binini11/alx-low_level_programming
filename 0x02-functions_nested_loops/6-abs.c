#include "main.h"
/**
*  * _abs- computes the absoute value of a number.
*  *@i: is an integer
*
*  * Return: Always 1 ,0
*/

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
