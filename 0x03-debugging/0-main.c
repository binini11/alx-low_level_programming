#include "main.h"
/**
*  * main - call a function and passes an input 0  to test out a function.
*
*  * Return: Always 0 (Success)
*/

int main(void)
{
	int i = 0;

	postitive_or_negative(i);
	return (0);
}
